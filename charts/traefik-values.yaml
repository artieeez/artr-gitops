service:
  enabled: true
  type: LoadBalancer
  annotations:
    service.beta.kubernetes.io/oci-load-balancer-type: "nlb"
entryPoints:
  web:
    address: ":8000"
  traefik:
    address: ":8080"
providers:
  kubernetesCRD:
    enabled: true
    allowCrossNamespace: true
api:
  dashboard: true
  insecure: true
logs:
  general:
    level: INFO
