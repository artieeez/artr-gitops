apiVersion: v1
kind: Service
metadata:
  name: sitio-dashboard-nginx
  namespace: sitio-staging
  labels:
    app: sitio-dashboard-nginx
    environment: sitio-staging
spec:
  type: ClusterIP
  selector:
    app: sitio-dashboard-nginx
  ports:
    - name: http
      port: 80
      targetPort: http
