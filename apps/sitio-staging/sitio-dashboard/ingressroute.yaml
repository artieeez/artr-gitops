apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sitio-dashboard
  namespace: sitio-staging
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`sitio-dashboard-staging.artr.com.br`)
      kind: Rule
      middlewares:
        - name: tinyauth-forwardauth
      services:
        - name: sitio-dashboard
          port: 80
  tls:
    secretName: wildcard-artr-com-br-tls
