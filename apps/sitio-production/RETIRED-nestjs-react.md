# NestJS / React stack — retired (M5 cutover)

These manifests are **no longer synced by ArgoCD**. The live app is `sitio-rails`
under `apps/sitio-*/sitio-rails/`.

Kept in git for archaeology only. Safe to delete this folder after you confirm
Rails is healthy on staging and production.
