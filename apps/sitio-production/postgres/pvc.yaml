apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sitio-production-postgres-data
  namespace: sitio-production
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
  storageClassName: nfs-client
