apiVersion: v1
kind: Service
metadata:
  name: home
  namespace: staging
  labels:
    app: home
spec:
  type: ClusterIP
  selector:
    app: home
  ports:
    - name: http
      port: 80
      targetPort: http
