apiVersion: v1
kind: PersistentVolume
metadata:
  name: staging-postgres-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""        # static NFS PV (no StorageClass)
  nfs:
    # Must match the ClusterIP of the nfs-server Service.
    server: 10.96.0.100
    path: /staging/postgre

