apiVersion: v1
kind: Service
metadata:
  name: nfs-server
  namespace: platform
spec:
  selector:
    app: nfs-server
  ports:
    - name: nfs
      port: 2049
      targetPort: 2049
  type: ClusterIP
  clusterIP: 10.96.0.100
