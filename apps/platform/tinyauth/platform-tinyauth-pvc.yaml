apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: platform-tinyauth-data
  namespace: platform
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  volumeName: platform-tinyauth-pv
  storageClassName: ""        # must match PV

