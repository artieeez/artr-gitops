apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: platform-filebrowser-data
  namespace: platform
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  volumeName: platform-filebrowser-pv
  storageClassName: ""

